{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "echo '📝 Code changed - consider running testing-agent next'"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check if code was written during this session (Edit or Write tools used on source files like .ts, .tsx, .js, .jsx, .prisma). If NO code was written, respond: {\"ok\": true}. If code WAS written but tests were not run, respond: {\"ok\": false, \"reason\": \"Tests not run after code changes\"}. Respond ONLY with valid JSON.",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
