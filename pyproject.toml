[project]
name = "the-hub-ai-scripts"
version = "0.1.0"
description = "Python scripts for The Hub AI"
authors = [
    {name = "Amihai Offen"}
]
readme = "README.md"
requires-python = "^3.9"
dependencies = [
    "openpyxl (>=3.1.5,<4.0.0)",
    "requests (>=2.32.5,<3.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
